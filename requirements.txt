# Real-Time Face Swap — Requirements
# Install with: pip install -r requirements.txt

opencv-python>=4.8.0
insightface>=0.7.3
onnxruntime>=1.16.0        # CPU 
onnxruntime-gpu>=1.16.0    # NVIDIA GPU (optional, install instead of CPU version)
pyvirtualcam>=0.10.0
numpy>=1.24.0
